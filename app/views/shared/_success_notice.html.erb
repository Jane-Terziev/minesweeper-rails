<% if flash[:success_notice].present? %>
  <div class="alert alert-success alert-dismissible fade show" role="alert">
    <% if flash[:success_notice].instance_of?(Array) %>
      <ul>
        <% flash[:success_notice].each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    <% else %>
      <p> <%= flash[:success_notice] %> </p>
    <% end %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>